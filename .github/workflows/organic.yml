name: microcomputer

on:
  push:

jobs:
  destination:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cycle: ["stale", "counsel", "commerce", "against", "treat"]
        cream: ["supervision", "entitle", "survivor", "tasteless"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar As.jar
